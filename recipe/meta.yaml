{% set name = "rfpimp" %}
{% set version = "1.2.1" %}
{% set sha256 = "ba3b985732dabaed4955e19d88753f6924b4db3fc1e8fd674b6766ee9509bb85" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - python
    - pip
  run:
    - python
    - numpy
    - pandas
    - scikit-learn
    - matplotlib

test:
  imports:
    - rfpimp

about:
  home: https://github.com/parrt/random-forest-importances
  license: MIT
  license_family: MIT
  summary: 'Code to compute permutation and drop-column importances in Python scikit-learn random forests'

extra:
  recipe-maintainers:
    - ericmjl
