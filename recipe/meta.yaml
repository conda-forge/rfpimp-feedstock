{% set name = "rfpimp" %}
{% set version = "1.3.6" %}
{% set sha256 = "22e37845ea691a464a1d19b5a350da9eaee7660bef09d6b7c9cf7bcc2249cfa8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - numpy
    - pandas
    - scikit-learn
    - matplotlib

test:
  imports:
    - rfpimp

about:
  home: https://github.com/parrt/random-forest-importances
  license: MIT
  license_family: MIT
  summary: Code to compute permutation and drop-column importances in Python scikit-learn random forests

extra:
  recipe-maintainers:
    - ericmjl
